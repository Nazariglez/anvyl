// @mode: check
// @frontend: new
// @expect: success

struct Point { x: int }

fn main() {
    let point = Point { x: 1 };
    let left: string = "point = " + point;
    let right: string = point + "!";
}
