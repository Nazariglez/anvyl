// @mode: check
// @frontend: new
// @expect: error
// @contains: immutable binding 'X' cannot be passed to var parameter

const X = 1;

fn bump(var x: int) {
    x += 1;
}

fn main() {
    bump(X);
}
