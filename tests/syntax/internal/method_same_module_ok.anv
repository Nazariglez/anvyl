// @mode: check
// @expect: success

struct Counter {
    value: int,

    @internal("do not call directly")
    fn reset(ref self) {
        self.value = 0;
    }

    fn increment(ref self) {
        self.value = self.value + 1;
    }
}

fn main() {
    var c = Counter { value: 0 };
    c.reset();
    c.increment();
}
