// @mode: check
// @expect: error
// @contains: same variable
// @frontend: new

fn swap(var a: int, var b: int) {
    let tmp = a;
    a = b;
    b = tmp;
}

fn make() -> fn() {
    var count = 0;
    || { swap(count, count); }
}
