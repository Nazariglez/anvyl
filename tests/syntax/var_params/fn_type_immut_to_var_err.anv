// @mode: check
// @frontend: new
// @expect: error
// @contains: mismatched types
// @skip: generic println awaits to_string/format AIR lowering

fn show(x: int) {
    println(x);
}

fn main() {
    let f: fn(var int) -> void = show;
}
