// @mode: check
// @expect: error
// @frontend: new
// @contains: cannot assign to immutable value 'x'

fn set(var x: int) { x = 1; }

fn main() {
    let f: fn(var int) -> void = set;
    let x = 0;
    f(x);
}
