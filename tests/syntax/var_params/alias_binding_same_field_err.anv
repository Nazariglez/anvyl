// @mode: check
// @expect: error
// @frontend: new
// @contains: same variable

struct Pair { a: int, b: int }

fn set(var x: int, var y: int) {
    x = y;
}

fn main() {
    var p = Pair { a: 1, b: 2 };
    var Pair { a } = p;
    set(a, p.a);
}
