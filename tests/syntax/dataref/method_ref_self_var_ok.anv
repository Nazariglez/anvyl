// @mode: check
// @expect: success
// @frontend: new

dataref Counter {
    value: int,
}

extend Counter {
    fn inc(ref self) { self.value = self.value + 1; }
}

fn main() {
    var c = Counter { value: 0 };
    c.inc();
}
