// @mode: check
// @frontend: new
// @expect: error
// @contains: static method

dataref Counter {
    value: int,

    fn new(start: int) -> Self {
        Counter { value: start }
    }
}

fn main() {
    let c = Counter { value: 1 };
    let f = c.new;
}
