// @mode: check
// @expect: error
// @contains: mutating method

dataref Counter {
    value: int,
}

extend Counter {
    fn inc(ref self) { self.value = self.value + 1; }
}

fn main() {
    let c = Counter { value: 0 };
    c.inc();
}
