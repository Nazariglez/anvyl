// @mode: check
// @expect: error
// @contains: Mismatched types: expected const '2', found '3'

fn same_len<T, N: int>(a: [T; N], b: [T; N]) -> T {
    a[0]
}

fn main() {
    same_len([1, 2, 3], [4, 5]);
}
