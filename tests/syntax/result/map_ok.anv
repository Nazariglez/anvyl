// @mode: check
// @expect: success

fn to_text(x: int) -> string {
    "value"
}

fn main() {
    let r: Result<int, string> = Result.Ok(2);
    let mapped = r.map(to_text);
    let _: Result<string, string> = mapped;
}
