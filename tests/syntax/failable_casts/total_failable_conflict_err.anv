// @mode: check
// @expect: error
// @contains: cast from and cast? from conflict

struct Token { raw: int }
struct Value { raw: int }

extend Value {
    cast from(token: Token) { Value { raw: token.raw } }
    cast? from(token: Token) { nil }
}

fn main() {}
