// @mode: check
// @expect: error
// @contains: invalid literal pattern

enum Anim: string {
    Idle = "idle",
}

fn main() {
    let anim = Anim.Idle;
    match anim {
        "idle" => {},
        _ => {},
    }
}
