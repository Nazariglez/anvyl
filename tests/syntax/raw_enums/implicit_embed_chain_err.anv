// @mode: check
// @expect: error
// @contains: mismatched types

enum State: int { Idle }
struct Wrapped { @as embed state: State }

fn test() {
    let wrapped = Wrapped { state: State.Idle };
    let raw: int = wrapped;
}
