// @mode: check
// @expect: success

enum State: int {
    Back = -1,
    Idle,
}

enum Anim: string {
    Idle = "idle",
}

fn main() {
    let n: int = State.Back as int;
    let s: string = Anim.Idle as string;
}
