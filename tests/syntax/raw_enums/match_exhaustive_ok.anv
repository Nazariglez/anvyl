// @mode: check
// @expect: success
// @frontend: new

enum State: int {
    Idle,
    Run,
}

fn main() {
    let state = State.Idle;
    match state {
        State.Idle => {},
        State.Run => {},
    }
}
