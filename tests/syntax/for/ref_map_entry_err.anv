// @mode: check
// @expect: error
// @frontend: new
// @contains: mutable map entry iteration is not supported

fn main() {
    var xs = ["a": 1];
    for ref entry in xs {
        entry = ("b", 2);
    }
}
