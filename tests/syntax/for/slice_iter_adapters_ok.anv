// @mode: check
// @expect: success

fn iterate(xs: slice[int]) {
    for i, el in iter(xs).skip(1).take(2).rev() {
        let _: int = i;
        let _: int = el;
    }
}

fn main() {
    let arr: [int; 4] = [1, 2, 3, 4];
    iterate(arr);
}
