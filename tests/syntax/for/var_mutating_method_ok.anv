// @mode: check
// @expect: success
// @frontend: new

struct Counter {
    n: int,

    fn inc(var self) {
        self.n += 1;
    }
}

fn main() {
    var counters = [Counter { n: 0 }];
    for var counter in counters {
        counter.inc();
    }
}
