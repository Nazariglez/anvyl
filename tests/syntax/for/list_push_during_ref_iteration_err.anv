// @mode: check
// @expect: error
// @contains: cannot change list shape while iterating it
// @frontend: new

fn main() {
    var xs: [int] = [1, 2, 3];
    for ref x in xs {
        xs.push(x);
    }
}
