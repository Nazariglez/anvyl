// @mode: check
// @expect: error
// @contains: ref iteration requires a mutable iterable place

fn main() {
    var xs = [1, 2, 3];
    for ref i, x in xs {
        let _: int = i;
        let _: int = x;
    }
}
