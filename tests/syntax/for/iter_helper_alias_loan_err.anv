// @mode: check
// @expect: error
// @contains: cannot change list shape while iterating it

fn items<S>(source: S) -> iter {
    let s = source;
    iter(s)
}

fn main() {
    var xs: [int] = [1, 2];
    for x in items(xs) {
        xs.push(x);
    }
}
