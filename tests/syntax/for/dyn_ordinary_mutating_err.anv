// @mode: check
// @expect: error
// @frontend: new
// @contains: mutating method

contract Updatable {
    fn update(var self);
}

struct Enemy {
    fn update(var self) {}
}

fn main() {
    var actors: [dyn Updatable] = [Enemy {}];
    for actor in actors {
        actor.update();
    }
}
