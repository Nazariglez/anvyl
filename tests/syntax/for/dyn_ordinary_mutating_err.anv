// @mode: check
// @expect: error
// @contains: mutating method

contract Updatable {
    fn update(ref self);
}

struct Enemy {
    fn update(ref self) {}
}

fn main() {
    var actors: [dyn Updatable] = [Enemy {}];
    for actor in actors {
        actor.update();
    }
}
