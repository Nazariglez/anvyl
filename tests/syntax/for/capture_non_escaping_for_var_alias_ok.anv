// @mode: check
// @expect: success
// @frontend: new

fn touch(var items: [int; 1]) {
    for var item in items {
        let f = || { item = 1; };
        f();
    }
}

fn main() {
    var items = [0];
    touch(items);
}
