// @mode: check
// @expect: error
// @contains: cannot change list shape while iterating it

fn items(source: [int]) -> iter {
    iter(source)
}

fn main() {
    var xs: [int] = [1, 2, 3];
    for x in items(xs) {
        xs.push(x);
    }
}
