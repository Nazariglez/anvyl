// @mode: check
// @expect: error
// @contains: cannot change list shape while iterating it

fn main() {
    var lists: [[int]] = [[1], [2]];
    let i = 0;
    for x in lists[i] {
        lists[i].push(x);
    }
}
