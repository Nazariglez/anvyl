// @mode: check
// @expect: error
// @contains: cannot change list shape while iterating it
// @frontend: new

fn main() {
    var lists: [[int]] = [[1], [2]];
    let i = 0;
    for x in lists[i] {
        lists[i].push(x);
    }
}
