// @mode: check
// @expect: error
// @contains: cannot pass iterated collection as mutable collection parameter

fn mutate(ref xs: [int]) {
    xs.push(1);
}

fn main() {
    var xs: [int] = [1, 2, 3];
    for x in xs {
        mutate(xs);
    }
}
