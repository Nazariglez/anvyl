// @mode: check
// @expect: success

fn main() {
    let arr: [int; 3] = [10, 20, 30];
    for el in iter(arr).rev() {
        let _: int = el;
    }

    let arr2: [int; 6] = [10, 20, 30, 40, 50, 60];
    for el in iter(arr2).step_by(2) {
        let _: int = el;
    }
}
