// @mode: check
// @expect: error
// @contains: cannot capture mutable loop alias 'item' in an escaping lambda
// @frontend: new

fn bad(var items: [int; 1]) -> fn() {
    for var item in items {
        return || { item = 1; };
    }
    || {}
}
