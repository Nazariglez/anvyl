// @mode: check
// @expect: error
// @contains: cannot change list shape while iterating it
// @frontend: new

fn main() {
    var lists: [[int]] = [[1], [2]];
    for x in lists[0] {
        lists[0].push(x);
    }
}
