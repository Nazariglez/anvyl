// @mode: check
// @expect: success
// @frontend: new

fn touch(ref items: [int; 1]) {
    for ref item in items {
        let f = || { item = 1; };
        f();
    }
}

fn main() {
    var items = [0];
    touch(items);
}
