// @mode: check
// @expect: success
// @frontend: new

contract Drawable {
    fn draw(self);
}

struct Enemy {
    fn draw(self) {}
}

fn main() {
    let actors: [dyn Drawable] = [Enemy {}];
    for actor in actors {
        actor.draw();
    }
}
