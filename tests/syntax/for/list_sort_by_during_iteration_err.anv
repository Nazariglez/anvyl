// @mode: check
// @expect: error
// @skip: sort_by is not clean core surface yet
// @contains: cannot change list shape while iterating it
// @frontend: new

fn main() {
    var xs: [int] = [3, 1, 2];
    for x in xs {
        xs.sort_by(|a, b| a < b);
    }
}
