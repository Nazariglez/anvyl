// @mode: check
// @expect: error
// @contains: ref iteration requires a mutable iterable place
// @frontend: new

fn main() {
    for ref i, x in 0..10 {
        let _: int = i;
        let _: int = x;
    }
}
