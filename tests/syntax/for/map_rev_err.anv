// @mode: check
// @expect: error
// @frontend: new
// @contains: rev is not supported for map iteration
// @skip: generic println awaits to_string/format AIR lowering

fn main() {
    let m = ["a": 1, "b": 2];
    for k, v in rev m {
        println(k);
    }
}
