// @mode: check
// @expect: success
// @frontend: new

fn bump(ref xs: slice[int]) {
    xs[0] += 1;
}

fn main() {
    var xs: [int] = [1, 2, 3];
    for x in xs {
        bump(xs);
        let _: int = x;
    }
}
