// @mode: check
// @expect: error
// @contains: cannot change list shape while iterating it

fn choose(flag: bool, a: [int], b: [int]) -> iter {
    if flag { iter(a) } else { iter(b) }
}

fn main() {
    var xs: [int] = [1];
    var ys: [int] = [2];
    let flag = false;
    for x in choose(flag, xs, ys) {
        ys.push(x);
    }
}
