// @mode: check
// @expect: success

contract Updatable {
    fn update(ref self);
}

struct Enemy {
    fn update(ref self) {}
}

fn main() {
    var actors: [dyn Updatable; 1] = [Enemy {}];
    for ref actor in actors {
        actor.update();
    }
}
