// @mode: check
// @expect: error
// @contains: cannot pass iterated collection as mutable collection parameter
// @frontend: new

fn replace<T>(var target: T, next: T) {
    target = next;
}

fn main() {
    var xs = [1, 2];
    for _ in xs {
        replace(xs, [3, 4]);
    }
}
