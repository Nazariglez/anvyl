// @mode: check
// @expect: error
// @contains: non-exhaustive match

enum Access: flag { Read, Write }

fn main() {
    let value = Access.Read;
    match value {
        .Read => {},
        .Write => {},
    }
}
