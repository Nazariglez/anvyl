// @mode: check
// @expect: error
// @contains: irrefutable pattern in let-else
// @skip: generic println awaits to_string/format AIR lowering

fn main() {
    let t = (1, 2);
    let (a, b) = t else { return; }
    println(a + b);
}
