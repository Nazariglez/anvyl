// @mode: check
// @expect: error
// @contains: requires an unwrapping pattern
// @skip: generic println awaits to_string/format AIR lowering

fn main() {
    let opt: int? = 42;
    if let x = opt {
        println(x);
    }
}
