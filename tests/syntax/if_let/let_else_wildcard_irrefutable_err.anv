// @mode: check
// @expect: error
// @contains: optional value requires an unwrapping pattern

fn process(opt: int?) -> int {
    let _ = opt else { return 0; }
    42
}

fn main() {
    let _r = process(nil);
}
