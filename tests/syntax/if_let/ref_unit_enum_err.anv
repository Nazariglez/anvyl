// @mode: check
// @expect: error
// @contains: ref alias patterns must bind an immediate optional or enum payload

enum State {
    Done,
}

fn main() {
    var state = State.Done;
    if ref State.Done = state {
    }
}
