// @mode: check
// @expect: error
// @frontend: new
// @contains: mutating method

extend<T> slice[T] {
    fn bump0(ref self) {
        self[0] = self[0];
    }
}

fn main() {
    var xs: [int] = [1, 2, 3, 4];
    xs[1..3].bump0();
}
