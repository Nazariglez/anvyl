// @mode: check
// @expect: error
// @frontend: new
// @contains: non-lvalue cannot be passed to ref parameter

fn mutate(ref xs: [int]) {}

fn main() {
    var xs: [int] = [1, 2, 3, 4];
    mutate(xs[1..3]);
}
