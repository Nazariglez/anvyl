// @mode: check
// @expect: error
// @frontend: new
// @contains: non-lvalue cannot be passed to var parameter

fn mutate(var xs: slice[int]) {}

fn main() {
    var xs: [int] = [1, 2, 3, 4];
    mutate(xs[1..3]);
}
