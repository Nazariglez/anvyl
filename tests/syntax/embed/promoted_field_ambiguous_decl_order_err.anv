// @mode: check
// @expect: error
// @frontend: new
// @contains: promoted field 'x' for type 'Enemy' is ambiguous: b.x, a.x

struct A { x: int }
struct B { x: int }

struct Enemy {
    embed b: B,
    embed a: A,
}

fn test(enemy: Enemy) {
    let _x = enemy.x;
}
