// @mode: check
// @expect: success

struct Entity { x: int }
struct Enemy { @as embed entity: Entity }
enum Event { Spawn(Entity) }

fn test() {
    let enemy = Enemy { entity: Entity { x: 1 } };
    let event = Event.Spawn(enemy);
}
