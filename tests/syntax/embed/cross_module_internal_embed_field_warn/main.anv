// @mode: check
// @expect: success
// @frontend: new
// @warn-contains: accessing internal field 'health' of type 'Enemy'

import lib { Enemy, enemy };

fn test() {
    let e: Enemy = enemy();
    println(e.hp);
}
