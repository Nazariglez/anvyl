// @mode: check
// @expect: error
// @contains: promoted field 'hp' is not a stored field of 'Enemy'; use stored path health.hp

struct Health { hp: int }
struct Enemy { embed health: Health }

fn test() {
    let _enemy = Enemy { hp: 10 };
}
