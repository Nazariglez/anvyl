// @mode: check
// @expect: success
// @frontend: new

struct A {
    fn tick(self) -> int { 1 }
}

struct Inner {
    embed a: A,
}

struct B {
    fn tick(self) -> int { 2 }
}

struct Enemy {
    embed inner: Inner,
    embed b: B,
}

fn test(enemy: Enemy) {
    let amount: int = enemy.tick();
}
