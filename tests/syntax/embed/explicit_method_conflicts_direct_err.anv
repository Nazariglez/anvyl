// @mode: check
// @expect: error
// @frontend: new
// @contains: explicit promoted method 'damage' conflicts with direct method on 'Enemy'

struct Health {
    fn damage(self) {}
}

struct Enemy {
    embed health: Health { fn damage },

    fn damage(self) {}
}
