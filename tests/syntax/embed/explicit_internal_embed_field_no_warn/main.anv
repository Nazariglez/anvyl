// @mode: check
// @expect: success
// @frontend: new
// @skip: generic println awaits to_string/format AIR lowering

import lib { Enemy, enemy };

fn test() {
    let e: Enemy = enemy();
    println(e.hp);
}
