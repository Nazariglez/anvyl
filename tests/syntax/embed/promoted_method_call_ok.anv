// @mode: check
// @expect: success

struct Health {
    hp: int,

    fn damage(self, amount: int) -> int {
        self.hp - amount
    }
}

struct Enemy {
    embed health: Health,
}

fn test(enemy: Enemy) {
    let hp: int = enemy.damage(3);
}
