// @mode: check
// @expect: success

struct Entity {
    x: int,
}

struct Enemy {
    entity: Entity,
}

fn move_entity(ref entity: Entity, dx: int) {
    entity.x += dx;
}

fn test() {
    var enemy = Enemy { entity: Entity { x: 1 } };
    move_entity(enemy.entity, 2);
}
