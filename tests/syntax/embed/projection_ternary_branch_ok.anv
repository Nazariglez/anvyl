// @mode: check
// @expect: success

struct Entity { x: int }
struct Enemy { @as embed entity: Entity }

fn test(flag: bool, left: Enemy, right: Enemy) {
    let entity: Entity = flag ? left : right;
}
