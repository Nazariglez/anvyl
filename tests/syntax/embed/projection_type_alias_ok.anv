// @mode: check
// @expect: success

struct Entity {
    x: int,
}

type Body = Entity;

struct Enemy {
    @as embed entity: Body,
}

fn move_entity(ref entity: Entity) {
    entity.x += 1;
}

fn test() {
    var enemy = Enemy { entity: Entity { x: 1 } };
    move_entity(enemy);
}
