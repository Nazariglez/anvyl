// @mode: check
// @expect: success

struct X { x: int }
struct Long { embed inner: X }
struct Short { x: string }

struct Outer {
    embed long: Long,
    embed short: Short,
}

fn test(outer: Outer) {
    let x: string = outer.x;
}
