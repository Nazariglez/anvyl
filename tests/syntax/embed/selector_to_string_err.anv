// @mode: check
// @expect: error
// @frontend: new
// @contains: `to_string` is not promoted; use the explicit path or define it on the outer type

struct Health {
    fn to_string(self) -> string { "health" }
}

struct Enemy {
    embed health: Health { fn to_string },
}
