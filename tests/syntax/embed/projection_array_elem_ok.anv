// @mode: check
// @expect: success

struct Entity { x: int }
struct Enemy { @as embed entity: Entity }

fn test() {
    let enemy = Enemy { entity: Entity { x: 1 } };
    let entities: [Entity; 1] = [enemy];
}
