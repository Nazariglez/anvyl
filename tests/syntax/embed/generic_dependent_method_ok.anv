// @mode: check
// @expect: success
// @frontend: new

struct Health {
    fn hp(self) -> int {
        1
    }
}

struct Box<T> {
    embed value: T,
}

fn test(box: Box<Health>) {
    let hp: int = box.hp();
}
