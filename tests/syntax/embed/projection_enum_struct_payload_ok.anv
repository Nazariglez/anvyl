// @mode: check
// @expect: success
// @frontend: new

struct Entity { x: int }
struct Enemy { @as embed entity: Entity }

enum Event {
    Hit { target: Entity },
}

fn test(enemy: Enemy) {
    let event = Event.Hit { target: enemy };
}
