// @mode: check
// @expect: success
// @frontend: new

struct Entity {
    x: int,
}

struct Enemy {
    @as embed entity: Entity,
}

fn move_entity(ref entity: Entity) {
    entity.x += 1;
}

fn test() {
    var enemy = Enemy { entity: Entity { x: 1 } };
    move_entity(enemy);
}
