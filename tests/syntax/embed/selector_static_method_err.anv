// @mode: check
// @expect: error
// @contains: embed method selector 'fn make' names a static method

struct Health {
    fn make() -> int { 1 }
}

struct Enemy {
    embed health: Health { fn make },
}
