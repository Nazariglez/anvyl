// @mode: check
// @expect: success

struct Entity {
    x: int,
}

struct Enemy {
    @as embed entity: Entity,
}

fn make_enemy() -> Enemy {
    Enemy { entity: Entity { x: 1 } }
}

fn take(entity: Entity) {}

fn test() {
    take(make_enemy());
}
