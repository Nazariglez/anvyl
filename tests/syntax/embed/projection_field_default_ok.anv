// @mode: check
// @expect: success

struct Entity { x: int }
struct Enemy { @as embed entity: Entity }

fn enemy() -> Enemy {
    Enemy { entity: Entity { x: 1 } }
}

struct Holder {
    entity: Entity = enemy()
}

fn test() {
    let holder = Holder {};
}
