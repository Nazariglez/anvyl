// @mode: check
// @expect: success
// @frontend: new

extern type HostStats {
    fn hp(self) -> int;
    fn damage(ref self, amount: int);
}

struct Enemy {
    embed stats: HostStats,
}

fn test(ref enemy: Enemy) {
    let hp: int = enemy.hp();
    enemy.damage(1);
}
