// @mode: check
// @expect: success

struct Health {
    hp: int,

    fn damage(ref self, amount: int) {
        self.hp -= amount;
    }
}

struct Enemy {
    embed health: Health,
}

fn test(ref enemy: Enemy) {
    enemy.damage(3);
}
