// @mode: check
// @expect: error
// @frontend: new
// @contains: duplicate `@as embed` projection target 'Entity' on 'Enemy'

struct Entity {
    id: int,
}

struct Enemy {
    @as embed primary: Entity,
    @as embed secondary: Entity,
}
