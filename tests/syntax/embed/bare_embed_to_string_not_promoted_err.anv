// @mode: check
// @expect: error
// @contains: Unknown method 'to_string'

struct Name { fn to_string(self) -> string { "n" } }
struct Player { embed name: Name }

fn main(p: Player) {
    p.to_string();
}
