// @mode: check
// @expect: error
// @contains: use stored path z, a explicitly

struct Target { x: int }
struct Enemy {
    embed z: Target,
    embed a: Target,
}

fn take(target: Target) {}

fn test(enemy: Enemy) {
    take(enemy);
}
