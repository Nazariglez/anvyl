// @mode: check
// @expect: error
// @frontend: new
// @contains: no direct `@as embed` projection from 'Enemy' to 'Entity'; use stored path entity explicitly

struct Entity {
    x: int,
}

struct Enemy {
    entity: Entity,
}

fn move_entity(var entity: Entity) {}

fn test() {
    var enemy = Enemy { entity: Entity { x: 1 } };
    move_entity(enemy);
}
