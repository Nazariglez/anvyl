// @mode: check
// @expect: success
// @frontend: new

struct Health { hp: int }

struct Enemy {
    @deprecated("use hp")
    embed health: Health,
}

fn test(enemy: Enemy) {
    println(enemy.hp);
}
