// @mode: check
// @expect: success

struct Health {
    fn damage(self, amount: int) -> int { amount }
}

struct Actor {
    embed health: Health,
}

struct Enemy {
    embed actor: Actor,
}

fn test(enemy: Enemy) {
    let amount: int = enemy.damage(3);
}
