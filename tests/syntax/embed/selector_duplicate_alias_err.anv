// @mode: check
// @expect: error
// @frontend: new
// @contains: duplicate embed field selector 'x'

struct Health { hp: int, max_hp: int }

struct Enemy {
    embed health: Health { hp as x, max_hp as x },
}
