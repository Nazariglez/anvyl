// @mode: check
// @expect: success
// @frontend: new

struct Local {
    x: int,
}

struct Global {
    y: int,
}

lazy let item = Global { y: 0 };

fn main() {
    let item = Local { x: 1 };
    let selected: int = item.x;
}
