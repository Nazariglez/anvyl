// @mode: check
// @expect: error
// @contains: try requires enclosing function to return Result or Option, found 'int'
// @frontend: new

fn bad(x: int?) -> int? {
    let f: fn() -> int = || { try x };
    f()
}
