// @mode: check
// @expect: success
// @frontend: new

fn id(ref x: int) -> ref int {
    x
}

fn apply(f: fn(ref int) -> ref int, ref x: int) -> ref int {
    f(x)
}

fn main() {
    var n = 1;
    apply(id, n) = 3;
}
