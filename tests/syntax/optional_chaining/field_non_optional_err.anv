// @mode: check
// @expect: error
// @contains: optional chaining requires an optional base type

struct Player {
    score: int,
}

fn main() {
    let p = Player { score: 100 };
    let s: int? = p?.score;
}

