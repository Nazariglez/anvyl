// @mode: check
// @expect: success
// @frontend: new
// @skip: generic println awaits to_string/format AIR lowering

lazy let value = {
    fn make() -> int {
        defer println(0);
        return 1;
    }
    make()
};

fn main() {
    let value: int = value;
}
