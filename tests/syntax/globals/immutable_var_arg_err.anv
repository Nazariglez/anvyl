// @mode: check
// @expect: error
// @frontend: new
// @contains: immutable binding 'config' cannot be passed to var parameter

lazy let config = 1;

fn set(var value: int) {
    value = 2;
}

fn main() {
    set(config);
}
