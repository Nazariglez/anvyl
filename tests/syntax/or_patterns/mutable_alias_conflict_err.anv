// @mode: check
// @expect: error
// @frontend: new
// @contains: ref arguments must not alias

fn touch_pair(ref p: (int, int), ref n: int) {}

fn main() {
    var pair = (1, 2);
    match ref pair {
        (x, _) | (_, x) => { touch_pair(pair, x); },
    }
}
