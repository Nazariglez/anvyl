// @mode: check
// @expect: success
// @frontend: new

fn tick() {}
fn keep(f: escaping fn()) {}

fn maybe(fs: [string: fn()]) -> fn()? {
    fs["tick"]
}

fn ok(fs: [string: fn()]) {
    keep(maybe(fs) ?? tick);
}
