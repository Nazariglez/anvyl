// @mode: check
// @expect: error
// @contains: cannot capture borrowed dynamic view 'actor' in an escaping lambda
// @frontend: new

contract Updatable {
    fn update(ref self);
}

struct Enemy {
    fn update(ref self) {}
}

fn bad(ref actor: dyn Updatable) -> fn() {
    || { actor.update(); }
}
