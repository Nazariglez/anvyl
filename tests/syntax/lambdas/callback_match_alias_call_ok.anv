// @mode: check
// @expect: success
// @frontend: new

fn run(f: fn()) {
    f();
}

fn main(f: fn(), opt: int?) {
    let g = match opt {
        Option.Some(_) => f,
        Option.None => f,
    };
    run(g);
}
