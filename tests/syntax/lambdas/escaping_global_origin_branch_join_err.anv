// @mode: check
// @expect: error
// @contains: function value with unknown storage origin cannot escape
// @frontend: new

lazy let stored: fn() -> int = || 1;

fn choose(cond: bool, seed: int) -> fn() -> int {
    var f: fn() -> int = || seed;
    if cond {
        f = stored;
    }
    f
}
