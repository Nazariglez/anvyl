// @mode: check
// @expect: error
// @contains: callback parameter 'f' is non-escaping by default, but it escapes here
// @frontend: new

fn bad(f: fn(), opt: int?) -> fn() {
    let g = match opt {
        Option.Some(_) => f,
        Option.None => || {},
    };
    g
}
