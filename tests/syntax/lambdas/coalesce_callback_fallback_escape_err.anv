// @mode: check
// @expect: error
// @contains: callback parameter 'cb' is non-escaping by default, but it escapes here

fn keep(f: escaping fn()) {}

fn bad(fs: [string: fn()], cb: fn()) {
    keep(fs["tick"] ?? cb);
}
