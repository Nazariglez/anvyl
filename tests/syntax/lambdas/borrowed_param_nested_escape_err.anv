// @mode: check
// @expect: error
// @contains: cannot capture borrowed parameter 'x' in an escaping lambda

fn make(ref x: int) -> fn() {
    let inner = || { x = 1; };
    let outer = || { inner(); };
    outer
}
