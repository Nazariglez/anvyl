// @mode: check
// @expect: error
// @contains: cannot capture mutable receiver 'self' in an escaping lambda
// @frontend: new

struct Counter {
    value: int,
}

extend Counter {
    fn bad(var self) -> fn() {
        || { self.value = 1; }
    }
}
