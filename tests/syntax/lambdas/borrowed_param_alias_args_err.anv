// @mode: check
// @expect: error
// @contains: var arguments must not alias the same variable
// @frontend: new

fn both(var a: int, var b: int) {
    a += 1;
    b += 1;
}

fn touch(var x: int) {
    let f = || {
        both(x, x);
    };
    f();
}
