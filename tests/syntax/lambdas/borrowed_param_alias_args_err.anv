// @mode: check
// @expect: error
// @contains: ref arguments must not alias the same variable

fn both(ref a: int, ref b: int) {
    a += 1;
    b += 1;
}

fn touch(ref x: int) {
    let f = || {
        both(x, x);
    };
    f();
}
