// @mode: check
// @expect: success
// @frontend: new

struct Player {
    hp: int,

    fn reset(var self) {
        self.hp = 0;
    }
}

fn main() {
    var p = Player { hp: 1 };
    let f = || { p.reset(); };
}
