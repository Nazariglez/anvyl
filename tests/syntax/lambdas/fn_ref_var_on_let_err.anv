// @mode: check
// @expect: error
// @contains: cannot assign to immutable value 'for_each'

fn mutate(var x: int) {
    x *= 2;
}

fn main() {
    let xs: [int] = [1, 2, 3];
    xs.for_each(mutate);
}
