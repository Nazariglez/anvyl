// @mode: check
// @expect: error
// @frontend: new
// @contains: extern functions cannot return callbacks

extern fn make() -> fn();

fn retain(f: escaping fn()) {
    f();
}

fn main() {
    retain(make());
}
