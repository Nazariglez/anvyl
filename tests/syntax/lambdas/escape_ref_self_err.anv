// @mode: check
// @expect: error
// @contains: cannot capture mutable receiver 'self' in an escaping lambda

struct Counter {
    value: int,
}

extend Counter {
    fn bad(ref self) -> fn() {
        || { self.value = 1; }
    }
}
