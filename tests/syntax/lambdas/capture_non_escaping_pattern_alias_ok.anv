// @mode: check
// @expect: success
// @frontend: new

struct Point {
    x: int,
    y: int,
}

fn touch(ref point: Point) {
    var Point { x } = point;
    let f = || { x = 1; };
    f();
}

fn main() {
    var p = Point { x: 0, y: 0 };
    touch(p);
}
