// @mode: check
// @expect: error
// @contains: cannot capture mutable pattern alias 'x' in an escaping lambda
// @frontend: new

struct Point {
    x: int,
    y: int,
}

fn bad(var point: Point) -> fn() {
    var Point { x } = point;
    || { x = 1; }
}
