// @mode: check
// @expect: success

fn first() {
    fn id<T>(x: T) -> T { x }
    let a: int = id(1);
}

fn second() {
    fn id<T>(x: T) -> T { x }
    let b: string = id("s");
}

fn main() {
    first();
    second();
}
