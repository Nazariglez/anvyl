// @mode: check
// @expect: success
// @frontend: new

fn id(var x: int) -> var int {
    x
}

fn apply(f: fn(var int) -> var int, var x: int) -> var int {
    f(x)
}

fn main() {
    var n = 1;
    apply(id, n) = 3;
}
