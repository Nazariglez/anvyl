// @mode: check
// @expect: error
// @contains: Unknown variant 'only_int'

enum Name: string {
    Bob = "bob",
}

extend<E> enum E: int {
    fn only_int() -> bool { true }
}

fn main() {
    let ok: bool = Name.only_int();
}
