// @mode: check
// @expect: success
// @frontend: new

enum Boxed<T> { Value(T) }

extend<E> enum E {
    fn same(self, other: E) -> bool { true }
}

fn main() {
    let a: Boxed<int> = Boxed.Value(1);
    let b: Boxed<int> = Boxed.Value(2);
    let ok: bool = a.same(b);
}
