// @mode: check
// @expect: error
// @frontend: new
// @contains: static method 'zero' must be called on type 'float'

extend float {
    fn zero() -> float { 0.0 }
}

fn main() {
    let x = 1.0.zero();
}
