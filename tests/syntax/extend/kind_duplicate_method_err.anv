// @mode: check
// @expect: error
// @contains: duplicate extend instance method 'tag'

extend<E> enum E {
    fn tag(self) -> string { "a" }
}

extend<E> enum E {
    fn tag(self) -> string { "b" }
}
