// @mode: check
// @expect: success

struct Player {
    hp: int,
}

extend<S> struct S {
    fn tag(self) -> string { "struct" }
}

fn main() {
    let p = Player { hp: 10 };
    let s: string = p.tag();
}
