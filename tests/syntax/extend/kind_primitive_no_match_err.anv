// @mode: check
// @expect: error
// @contains: Unknown method 'tag' for type 'int'

extend<E> enum E {
    fn tag(self) -> string { "enum" }
}

fn main() {
    let n = 1;
    let s: string = n.tag();
}
