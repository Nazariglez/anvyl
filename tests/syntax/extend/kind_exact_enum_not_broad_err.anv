// @mode: check
// @expect: error
// @frontend: new
// @contains: Unknown method 'tag' for type 'Other'

enum State { Idle }
enum Other { One }

extend enum State {
    fn tag(self) -> int { 1 }
}

fn main() {
    let o: Other = Other.One;
    o.tag();
}
