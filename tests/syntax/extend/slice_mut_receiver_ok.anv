// @mode: check
// @expect: success

extend<T> slice[T] {
    fn swap(ref self, a: int, b: int) {
        let tmp = self[a];
        self[a] = self[b];
        self[b] = tmp;
    }
}

fn use(ref xs: slice[int]) {
    xs.swap(0, 1);
}

fn main() {}
