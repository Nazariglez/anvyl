// @mode: check
// @expect: success

enum State {
    Idle,
    Run,
}

extend<E> enum E {
    fn same(self, other: E) -> bool { true }
}

fn main() {
    let a: State = State.Idle;
    let b: State = State.Run;
    let ok: bool = a.same(b);
}
