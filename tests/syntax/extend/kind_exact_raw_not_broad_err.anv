// @mode: check
// @expect: error
// @contains: Unknown method 'tag' for type 'OtherCode'

enum Code: int { Ok = 0 }
enum OtherCode: int { Bad = 1 }

extend enum Code: int {
    fn tag(self) -> int { 1 }
}

fn main() {
    let c: OtherCode = OtherCode.Bad;
    c.tag();
}
