// @mode: check
// @expect: error
// @contains: static method 'Open' already exists for type 'Mode'

import base { Mode };
import exts { keep };

fn main() {
    keep();
    let m: Mode = Mode.Open;
}
