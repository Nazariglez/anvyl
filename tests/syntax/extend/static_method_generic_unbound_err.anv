// @mode: check
// @expect: error
// @contains: Could not infer type parameter 'T'

struct Box<T> {
    value: T,
}

extend<T> Box<T> {
    fn make() -> Box<T> { Box { value: 0 } }
}

fn main() {
    Box.make();
}
