// @mode: check
// @expect: error
// @frontend: new
// @contains: Unknown method 'tag' for type 'Box'

struct Box<T> {
    item: T,
}

extend<S> struct S {
    fn tag() -> string { "struct" }
}

fn main() {
    let label: string = Box.tag();
}
