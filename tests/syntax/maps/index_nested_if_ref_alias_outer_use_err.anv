// @mode: check
// @expect: error
// @frontend: new
// @contains: mutable alias binding

fn main() {
    var groups: [string: [string: int]] = ["a": ["x": 1]];
    if ref inner? = groups["a"] {
        if ref n? = inner["x"] {
            inner["y"] = 2;
            n += 1;
        }
    }
}
