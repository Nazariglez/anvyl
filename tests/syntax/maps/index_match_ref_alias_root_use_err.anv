// @mode: check
// @expect: error
// @frontend: new
// @contains: mutable alias binding

fn main() {
    var counts: [string: int] = ["a": 1];
    match ref counts["a"] {
        value? => {
            counts["b"] = 2;
            value = 3;
        },
        nil => {},
    }
}
