// @mode: check
// @expect: error
// @frontend: new
// @contains: var pattern requires a mutable place

fn main() {
    var counts: [string: int] = ["a": 1];
    if var value? = counts["a"] {
        println(value);
    }
}
