// @mode: check
// @expect: error
// @contains: mutable alias binding

fn main() {
    var counts: [string: int] = ["a": 1];
    if ref value? = counts["a"] {
        counts["b"] = 2;
        value = 2;
    }
}
