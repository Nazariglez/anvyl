// @mode: check
// @expect: success

extend int? {
    fn unwrap_or(self, default: int) -> int {
        match self { v? => v, nil => default }
    }
}

fn get(m: [string: int], key: string) -> int {
    m[key].unwrap_or(0)
}
