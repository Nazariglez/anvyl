// @mode: check
// @expect: error
// @contains: map entry aliases require a `?` payload pattern

fn main() {
    var counts: [string: int] = ["a": 1];
    match ref counts["a"] {
        _ => {},
    }
}
