// @mode: check
// @expect: error
// @contains: conflicting inferred dynamic requirement 'f'

struct Actor {
    fn f(self, x: int) {}
}

fn use_actor(actor: dyn _) {
    actor.f(1);
    actor.f("x");
}

fn main() {
    use_actor(Actor {});
}
