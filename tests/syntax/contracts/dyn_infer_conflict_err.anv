// @mode: check
// @expect: error
// @frontend: new
// @contains: conflicting inferred dynamic requirement 'f'

struct Actor {
    fn f(self, x: int) {}
}

fn main() {
    let actor: dyn _ = Actor {};
    actor.f(1);
    actor.f("x");
}
