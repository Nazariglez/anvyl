// @mode: check
// @expect: error
// @frontend: new
// @contains: exact downcast is only supported in conditional bindings

contract Drawable {
    fn draw(self);
}

struct Actor {
    fn draw(self) {}
}

fn main() {
    let actor: dyn Drawable = Actor {};
    let enemy = actor as? Actor;
}
