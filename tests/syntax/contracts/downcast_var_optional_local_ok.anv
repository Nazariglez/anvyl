// @mode: check
// @expect: success

contract Drawable {
    fn draw(self);
}

struct Enemy {
    hp: int,

    fn draw(self) {}
}

fn main() {
    var actor: dyn Drawable = Enemy { hp: 10 };
    if var enemy? = actor as? Enemy {
        enemy.hp = 1;
    }
    actor.draw();
}
