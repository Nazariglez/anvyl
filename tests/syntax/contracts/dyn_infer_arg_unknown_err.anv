// @mode: check
// @expect: error
// @contains: cannot infer parameter type for dynamic method 'f'

struct Actor {
    fn f(self, x: int) {}
}

fn use_actor(actor: dyn _) {
    actor.f(nil);
}

fn main() {
    use_actor(Actor {});
}
