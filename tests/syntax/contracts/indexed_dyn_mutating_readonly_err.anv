// @mode: check
// @expect: error
// @frontend: new
// @contains: mutating method

contract Updatable {
    fn update(ref self);
}

struct Enemy {
    fn update(ref self) {}
}

fn main() {
    let items: [dyn Updatable] = [Enemy {}];
    items[0].update();
}
