// @mode: check
// @expect: error
// @contains: mutating method

contract Updatable {
    fn update(ref self);
}

struct Enemy {
    fn update(ref self) {}
}

fn main() {
    let items: [dyn Updatable] = [Enemy {}];
    items[0].update();
}
