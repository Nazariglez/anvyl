// @mode: check
// @expect: error
// @frontend: new
// @contains: Mismatched types

contract Drawable {
    fn draw(self);
}

struct Sprite {
    fn draw(self) {}
}

fn main() {
    let sprites: [Sprite] = [Sprite {}];
    let items: [dyn Drawable] = sprites;
}
