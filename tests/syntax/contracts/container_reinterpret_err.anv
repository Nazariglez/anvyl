// @mode: check
// @expect: error
// @contains: collection conversion is not implicit

contract Drawable {
    fn draw(self);
}

struct Sprite {
    fn draw(self) {}
}

fn main() {
    let sprites: [Sprite] = [Sprite {}];
    let items: [dyn Drawable] = sprites;
}
