// @mode: check
// @expect: success
// @frontend: new

contract Action {
    fn act(self);
}

struct Actor {
    fn act(self) {}
}

fn main() {
    let action: dyn Action = Actor {};
    let text = f"{action}";
}
