// @mode: check
// @expect: success
// @frontend: new

struct Actor {
    fn update(ref self, dt: float) {}
}

fn tick(ref actor: dyn _) {
    actor.update(1.0);
}

fn main() {
    var actor = Actor {};
    tick(actor);
}
