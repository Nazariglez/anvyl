// @mode: check
// @expect: error
// @frontend: new
// @contains: exact downcast source must be a dynamic value

struct Enemy {}

fn main() {
    let actor = Enemy {};
    if let enemy = actor as? Enemy {}
}
