// @mode: check
// @expect: success

contract Updatable {
    fn update(ref self, dt: float);
}

struct Enemy {
    hp: int,

    fn update(ref self, dt: float) {
        self.hp -= 1;
    }
}

fn main() {
    var actor: dyn Updatable = Enemy { hp: 10 };
    actor.update(1.0);
}
