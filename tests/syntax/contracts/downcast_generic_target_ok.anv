// @mode: check
// @expect: success

contract Drawable {
    fn draw(self);
}

struct Enemy {
    fn draw(self) {}
}

fn try_down<T>(actor: dyn Drawable) {
    if let value? = actor as? T {
        value.draw();
    }
}

fn main() {
    try_down<Enemy>(Enemy {});
}
