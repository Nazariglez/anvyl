// @mode: check
// @expect: error
// @contains: dynamic match source must be a dynamic value

struct Enemy { fn draw(self) {} }

fn main() {
    let actor = Enemy {};
    match actor as? {
        Enemy(enemy) => enemy.draw(),
        other => other.draw(),
    };
}
