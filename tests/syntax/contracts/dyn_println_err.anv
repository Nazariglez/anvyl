// @mode: check
// @expect: error
// @contains: dynamic values cannot be formatted

contract Action {
    fn act(self);
}

struct Actor {
    fn act(self) {}
}

fn main() {
    let action: dyn Action = Actor {};
    println(action);
}
