// @mode: check
// @expect: success
// @frontend: new

struct Actor {
    fn draw(self) {}
}

struct System {
    fn tick(actor: dyn _) {
        actor.draw();
    }
}

fn main() {
    System.tick(Actor {});
}
