// @mode: check
// @expect: error
// @frontend: new
// @contains: does not satisfy contract bound 'Updatable'

contract Updatable {
    fn update(ref self, dt: float);
}

struct Rock {}

fn tick<T: Updatable>(actor: T, dt: float) {}

fn main() {
    tick(Rock {}, 1.0);
}
