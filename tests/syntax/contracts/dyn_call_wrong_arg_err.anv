// @mode: check
// @expect: error
// @frontend: new
// @contains: mismatched types

contract Updatable {
    fn update(var self, dt: float);
}

struct Enemy {
    fn update(var self, dt: float) {}
}

fn main() {
    var actor: dyn Updatable = Enemy {};
    actor.update("bad");
}
