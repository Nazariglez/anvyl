// @mode: check
// @expect: error
// @contains: mismatched types

contract Updatable {
    fn update(ref self, dt: float);
}

struct Enemy {
    fn update(ref self, dt: float) {}
}

fn main() {
    var actor: dyn Updatable = Enemy {};
    actor.update("bad");
}
