// @mode: check
// @expect: error
// @frontend: new
// @contains: fixed-array conversion is not implicit

contract Drawable {
    fn draw(self);
}

struct Sprite {
    fn draw(self) {}
}

fn main() {
    let sprites: [Sprite; 1] = [Sprite {}];
    let items: [dyn Drawable; 1] = sprites;
}
