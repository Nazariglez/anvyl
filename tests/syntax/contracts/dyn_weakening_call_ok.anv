// @mode: check
// @expect: success
// @frontend: new

contract A {
    fn a(self);
}

contract B {
    fn b(self);
}

struct Both {
    fn a(self) {}
    fn b(self) {}
}

fn take_a(value: dyn A) {
    value.a();
}

fn main() {
    let both: dyn A + B = Both {};
    take_a(both);
}
