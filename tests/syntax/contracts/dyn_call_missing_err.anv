// @mode: check
// @expect: error
// @contains: dynamic contract 'Drawable' has no method 'update'

contract Drawable {
    fn draw(self);
}

struct Sprite {
    fn draw(self) {}
    fn update(ref self) {}
}

fn main() {
    var actor: dyn Drawable = Sprite {};
    actor.update();
}
