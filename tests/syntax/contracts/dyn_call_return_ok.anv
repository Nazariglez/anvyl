// @mode: check
// @expect: success

contract Named {
    fn name(self) -> string;
}

struct Enemy {
    fn name(self) -> string {
        "enemy"
    }
}

fn main() {
    let actor: dyn Named = Enemy {};
    let name: string = actor.name();
}
