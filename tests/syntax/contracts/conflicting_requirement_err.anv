// @mode: check
// @expect: error
// @frontend: new
// @contains: conflicting contract requirement 'draw' in contract 'Drawable'

contract Drawable {
    fn draw(self);
    fn draw(self, layer: int);
}
