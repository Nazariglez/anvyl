// @mode: check
// @expect: error
// @frontend: new
// @contains: dynamic root cannot be used while a mutable downcast binding is live

contract Drawable {
    fn draw(self);
}

struct Enemy {
    hp: int,

    fn draw(self) {}
}

struct Slot {
    actor: dyn Drawable,
    count: int,
}

fn main() {
    var slot = Slot { actor: Enemy { hp: 1 }, count: 0 };
    if let var enemy = slot.actor as? Enemy {
        slot.actor.draw();
    }
}
