// @mode: check
// @expect: error
// @contains: does not satisfy contract bound 'Named'

contract Named {
    fn name(self) -> string;
}

struct Box<T: Named> {
    value: T,
}

struct Wrapper<U> {
    box: Box<U>,
}

fn main() {}
