// @mode: check
// @expect: error
// @frontend: new
// @contains: Mismatched types

contract Drawable {
    fn draw(self);
}

struct Sprite {
    fn draw(self) {}
}

fn main() {
    let maybe: Sprite? = Sprite {};
    let item: dyn Drawable = maybe;
}
