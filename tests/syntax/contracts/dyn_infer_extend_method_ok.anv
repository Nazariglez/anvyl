// @mode: check
// @expect: success

struct Actor {
    fn draw(self) {}
}

struct System {}

extend System {
    fn tick(self, actor: dyn _) {
        actor.draw();
    }
}

fn main() {
    System {}.tick(Actor {});
}
