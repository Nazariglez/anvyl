// @mode: check
// @expect: error
// @contains: mutating method requires mutable receiver 'update'

contract Updatable {
    fn update(ref self, dt: float);
}

struct Enemy {
    fn update(ref self, dt: float) {}
}

fn main() {
    let actor: dyn Updatable = Enemy {};
    actor.update(1.0);
}
