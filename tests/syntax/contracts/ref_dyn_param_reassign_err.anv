// @mode: check
// @expect: error
// @frontend: new
// @contains: cannot reassign borrowed dynamic parameter 'actor'

contract Updatable {
    fn update(ref self, dt: float);
}

struct Enemy {
    fn update(ref self, dt: float) {}
}

fn tick(ref actor: dyn Updatable) {
    actor = Enemy {};
}
