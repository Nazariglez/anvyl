// @mode: check
// @expect: success
// @frontend: new

contract Drawable {
    fn draw(self);
}

struct Enemy {
    fn draw(self) {}
    fn attack(self) {}
}

fn main() {
    let actor: dyn Drawable = Enemy {};
    if let enemy = actor as? Enemy {
        enemy.attack();
    }
}
