// @mode: check
// @expect: error
// @contains: slices are invariant

contract Drawable {
    fn draw(self);
}

struct Sprite {
    fn draw(self) {}
}

fn take(items: slice[dyn Drawable]) {}

fn main() {
    let sprites: [Sprite; 1] = [Sprite {}];
    take(sprites);
}
