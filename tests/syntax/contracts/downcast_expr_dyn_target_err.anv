// @mode: check
// @expect: error
// @frontend: new
// @contains: downcast tests the stored concrete type

contract Drawable {
    fn draw(self);
}

struct Actor {
    fn draw(self) {}
}

fn main() {
    let actor: dyn Drawable = Actor {};
    let enemy = actor as? dyn Drawable;
}
