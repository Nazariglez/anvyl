// @mode: check
// @expect: success
// @frontend: new

struct Actor {
    fn draw(self) {}
}

fn use<T>(value: T) {
    let actor: dyn _ = value;
    actor.draw();
}

fn main() {
    use(Actor {});
}
