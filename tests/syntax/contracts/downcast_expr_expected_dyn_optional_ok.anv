// @mode: check
// @expect: success
// @frontend: new

contract Drawable {
    fn draw(self);
}

struct Enemy {
    fn draw(self) {}
}

fn main() {
    let actor: dyn Drawable = Enemy {};
    let drawable: dyn Drawable? = actor as? Enemy;
}
