// @mode: check
// @expect: error
// @contains: exact downcast currently binds a single identifier

contract Drawable {
    fn draw(self);
}

struct Enemy {
    fn draw(self) {}
}

fn main() {
    let actor: dyn Drawable = Enemy {};
    if ref _? = actor as? Enemy {}
}
