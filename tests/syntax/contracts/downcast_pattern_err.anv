// @mode: check
// @expect: error
// @contains: dynamic downcast binding requires a single identifier

contract Drawable {
    fn draw(self);
}

struct Enemy {
    fn draw(self) {}
}

fn main() {
    let actor: dyn Drawable = Enemy {};
    if ref _? = actor as? Enemy {}
}
