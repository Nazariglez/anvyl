// @mode: check
// @expect: error
// @frontend: new
// @contains: cannot infer return type for dynamic method 'count'

struct Actor {
    fn count(self) -> int { 1 }
}

fn use_actor(actor: dyn _) {
    let count = actor.count(); // needs return type to infer the contract
}

fn main() {
    use_actor(Actor {});
}
