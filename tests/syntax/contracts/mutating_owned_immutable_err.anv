// @mode: check
// @expect: error
// @contains: immutable

contract Counter {
    fn bump(ref self);
}

struct Score {
    points: int,

    fn bump(ref self) { self.points += 1; }
}

fn main() {
    let counter: dyn Counter = Score { points: 1 };
    counter.bump();
}
