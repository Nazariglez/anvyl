// @mode: check
// @expect: error
// @contains: ref pattern requires a mutable place

contract Drawable {
    fn draw(self);
}

struct Enemy {
    fn draw(self) {}
}

fn make_actor() -> dyn Drawable {
    Enemy {}
}

fn main() {
    if ref enemy = make_actor() as? Enemy {}
}
