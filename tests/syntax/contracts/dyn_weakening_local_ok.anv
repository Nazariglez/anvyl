// @mode: check
// @expect: success
// @frontend: new

contract A {
    fn a(self);
}

contract B {
    fn b(self);
}

struct Both {
    fn a(self) {}
    fn b(self) {}
}

fn main() {
    let both: dyn A + B = Both {};
    let only_a: dyn A = both;
    only_a.a();
}
