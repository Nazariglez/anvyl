// @mode: check
// @expect: success

contract Updatable {
    fn update(ref self);
}

struct Enemy {
    fn update(ref self) {}
}

fn main() {
    var items: [dyn Updatable] = [Enemy {}];
    items[0].update();
}
