// @mode: check
// @expect: error
// @frontend: new
// @contains: expected `Enemy`, found `Enemy?`

contract Drawable {
    fn draw(self);
}

struct Enemy {
    fn draw(self) {}
}

fn main() {
    let actor: dyn Drawable = Enemy {};
    let enemy: Enemy = actor as? Enemy;
}
