// @mode: check
// @expect: error
// @frontend: new
// @contains: immutable

contract Drawable {
    fn draw(self);
}

struct Enemy {
    fn draw(self) {}
}

fn make_actor() -> dyn Drawable {
    Enemy {}
}

fn main() {
    if let var enemy = make_actor() as? Enemy {}
}
