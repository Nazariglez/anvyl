// @mode: check
// @expect: error
// @frontend: new
// @contains: inferred dynamic contracts are only allowed as direct parameters of callables with bodies

struct Actor {
    fn draw(self) {}
}

fn make_actor() -> dyn _ {
    Actor {}
}
