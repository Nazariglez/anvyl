// @mode: check
// @expect: error
// @contains: duplicate dynamic match target

contract Drawable { fn draw(self); }
struct Enemy { fn draw(self) {} }

fn main() {
    let actor: dyn Drawable = Enemy {};
    match actor as? {
        Enemy(first) => first.draw(),
        Enemy(second) => second.draw(),
        other => other.draw(),
    };
}
