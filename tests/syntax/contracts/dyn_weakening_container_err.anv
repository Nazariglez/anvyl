// @mode: check
// @expect: error
// @contains: dynamic weakening applies to values, not containers

contract A {
    fn a(self);
}

contract B {
    fn b(self);
}

fn use_list(values: [dyn A]) {}

fn main() {
    let values: [dyn A + B] = [];
    use_list(values);
}
