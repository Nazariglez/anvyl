// @mode: check
// @expect: error
// @contains: dynamic match fallback arm must be last

contract Drawable { fn draw(self); }
struct Enemy { fn draw(self) {} }

fn main() {
    let actor: dyn Drawable = Enemy {};
    match actor as? {
        other => other.draw(),
        Enemy(enemy) => enemy.draw(),
    };
}
