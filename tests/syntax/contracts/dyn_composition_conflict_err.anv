// @mode: check
// @expect: error
// @frontend: new
// @contains: conflicting contract requirement

contract A {
    fn draw(self);
}

contract B {
    fn draw(self, layer: int);
}

fn render(actor: dyn A + B) {}

fn main() {}
