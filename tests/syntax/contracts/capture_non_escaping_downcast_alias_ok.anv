// @mode: check
// @expect: success
// @frontend: new

contract Updatable {
    fn update(ref self);
}

struct Enemy {
    hp: int,

    fn update(ref self) {}
}

fn touch(ref actor: dyn Updatable) {
    if ref enemy = actor as? Enemy {
        let f = || { enemy.hp = 1; };
        f();
    }
}

fn main() {
    var actor: dyn Updatable = Enemy { hp: 0 };
    touch(actor);
}
