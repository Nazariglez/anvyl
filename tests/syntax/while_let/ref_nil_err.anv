// @mode: check
// @expect: error
// @contains: ref alias patterns must bind an immediate optional or enum payload
// @frontend: new

fn main() {
    var opt: int? = nil;
    while ref nil = opt {
        break;
    }
}
