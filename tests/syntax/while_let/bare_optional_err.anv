// @mode: check
// @expect: error
// @contains: requires an unwrapping pattern
// @skip: stringify not support for optional yet

fn next() -> int? {
    nil
}

fn main() {
    while let val = next() {
        println(val);
    }
}
