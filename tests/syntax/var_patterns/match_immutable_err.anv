// @mode: check
// @expect: error
// @contains: ref pattern requires a mutable place
// @frontend: new

enum Color { Red, Green, Blue }

fn main() {
    let c = Color.Red;
    match ref c {
        Color.Red => { println("red"); },
        other => { println("other"); },
    }
}
