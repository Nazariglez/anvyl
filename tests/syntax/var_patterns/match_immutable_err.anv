// @mode: check
// @expect: error
// @contains: var pattern requires a mutable place
// @frontend: new

enum Color { Red, Green, Blue }

fn main() {
    let c = Color.Red;
    match var c {
        Color.Red => { println("red"); },
        other => { println("other"); },
    }
}
