// @mode: check
// @expect: error
// @frontend: new
// @contains: no direct `@as embed` projection from 'float' to 'Vec2'

struct Vec2 { x: float, y: float }

extend Vec2 {
    cast from(v: float) { Vec2 { x: v, y: v } }
}

fn touch(var v: as Vec2) {}

fn main() {
    var x = 1.0;
    touch(x);
}
