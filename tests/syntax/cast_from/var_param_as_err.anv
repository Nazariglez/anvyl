// @mode: check
// @expect: error
// @frontend: new
// @contains: `as` cannot be combined with `var`; use `var x: T`

fn touch(var x: as int) {
    x = x + 1;
}

fn main() {
    var x = 1;
    touch(x);
}
