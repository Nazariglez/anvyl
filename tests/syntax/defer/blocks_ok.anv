// @mode: check
// @expect: success
// @skip: generic println awaits to_string/format AIR lowering

fn main() {
    defer println("cleanup");
    defer {
        let x = 1;
        println(x);
    }

    defer {
        defer println("inner");
        println("outer");
    }
}
