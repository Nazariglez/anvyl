// @mode: check
// @expect: error
// @frontend: new
// @contains: same variable

fn set(ref x: int, ref y: int) {
    x = 1;
    y = 2;
}

fn main() {
    var pair = (0, 0);
    set(pair.0, pair.0);
}
