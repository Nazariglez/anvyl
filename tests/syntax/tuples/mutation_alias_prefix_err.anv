// @mode: check
// @expect: error
// @contains: same variable

fn take(ref pair: (int, int), ref x: int) {
    pair.0 = x;
}

fn main() {
    var nested = ((1, 2), 3);
    take(nested.0, nested.0.1);
}
