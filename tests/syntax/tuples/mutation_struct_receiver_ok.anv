// @mode: check
// @expect: success
// @frontend: new

struct Counter { value: int }

extend Counter {
    fn inc(ref self) {
        self.value = self.value + 1;
    }
}

fn main() {
    var pair = (Counter { value: 0 }, 2);
    pair.0.value = 3;
    pair.0.inc();
}
