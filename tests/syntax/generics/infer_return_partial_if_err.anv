// @mode: check
// @expect: error
// @frontend: new
// @contains: inferred return type mismatch

fn f<T>(x: T, cond: bool) -> _ {
    if cond {
        return x;
    }
}

fn main() {
    f(1, false);
}
