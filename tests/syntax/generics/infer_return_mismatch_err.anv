// @mode: check
// @expect: error
// @frontend: new
// @contains: inferred return type mismatch

fn choose<T>(x: T) -> _ {
    if true {
        return x;
    }
    return 1;
}

fn main() {
    choose("hi");
}
