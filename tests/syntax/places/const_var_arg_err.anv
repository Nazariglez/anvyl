// @mode: check
// @frontend: new
// @expect: error
// @contains: immutable binding 'VALUE' cannot be passed to var parameter

const VALUE = 1;

fn bump(var value: int) {
    value += 1;
}

fn main() {
    bump(VALUE);
}
