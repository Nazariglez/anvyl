// @mode: check
// @expect: error
// @contains: immutable binding 'VALUE' cannot be passed to ref parameter

const VALUE = 1;

fn bump(ref value: int) {
    value += 1;
}

fn main() {
    bump(VALUE);
}
