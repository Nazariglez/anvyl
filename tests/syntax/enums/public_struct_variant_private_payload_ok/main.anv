// @mode: check
// @expect: success
// @frontend: new

import m { Widget, hidden, take };

fn main() {
    let event = Widget.Reveal { secret: hidden() };
    match event { Widget.Reveal { secret } => take(secret) }
}
