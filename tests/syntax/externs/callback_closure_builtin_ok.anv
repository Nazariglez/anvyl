// @mode: check
// @expect: success
// @frontend: new
// @skip: clean frontend lambda callback checking is deferred

extern fn apply(value: int, cb: fn(int) -> void);

// check that externs can call builtins
fn main() {
    apply(5, |x| println(x * x));
    apply(5, |x| assert(x > 0));
}
