// @mode: check
// @expect: error
// @frontend: new
// @contains: Unknown method 'move_by'

extern type Point {
    x: float;
    y: float;
    fn new(x: float, y: float) -> Self;
    fn move_by(var self, dx: float, dy: float);
}

fn main() {
    Point.move_by(1.0, 2.0);
}
