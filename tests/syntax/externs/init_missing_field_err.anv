// @mode: check
// @expect: error
// @frontend: new
// @contains: Missing field 'y'

extern type Point rep inline {
    init(x: float, y: float);
    x: float;
    y: float;
}

fn main() {
    let p = Point { x: 1.0 };
}
