// @mode: check
// @expect: error
// @frontend: new
// @contains: cannot assign to immutable value 'x'

extern type Point rep inline {
    computed x: int;
    init;
}

fn main() {
    let p = Point {};
    p.x = 1;
}
