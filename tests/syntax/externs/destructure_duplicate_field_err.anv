// @mode: check
// @expect: error
// @frontend: new
// @contains: Duplicate field 'x'

extern type Point rep inline {
    init;
    x: float;
    y: float;
}

fn main() {
    let p = Point { x: 1.0, y: 2.0 };
    let Point { x, x } = p;
}
