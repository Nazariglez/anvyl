// @mode: check
// @expect: error
// @contains: operator `+` cannot be applied

extern type Vec2 rep inline {
    x: float;
    y: float;
    init;
    op Self + Self -> Self;
}

fn main() {
    let a = Vec2 { x: 1.0, y: 2.0 };
    let b = a + 2.0;
}
