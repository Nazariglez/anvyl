// @mode: check
// @expect: error
// @frontend: new
// @contains: Mismatched types

extern fn native_apply(value: int, cb: fn(int) -> int) -> int;

fn main() {
    let result = native_apply(5, 42);
}
