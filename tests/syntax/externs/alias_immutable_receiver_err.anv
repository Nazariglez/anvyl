// @mode: check
// @expect: error
// @contains: var pattern requires a mutable place
// @frontend: new

extern type Point {
    x: float;
    y: float;
}

fn main(p: Point) {
    var Point { x } = p;
}
