// @mode: check
// @expect: success
// @frontend: new

extern type Point;
extern fn use_point(point: Point) -> void;

struct Holder {
    @as embed point: Point,
}

fn test(point: Point) {
    let holder = Holder { point };
    use_point(holder);
}
