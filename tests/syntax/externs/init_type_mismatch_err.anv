// @mode: check
// @expect: error
// @frontend: new
// @contains: mismatched types

extern type Point rep inline {
    init;
    x: float;
    y: float;
}

fn main() {
    let p = Point { x: "hello", y: 2.0 };
}
