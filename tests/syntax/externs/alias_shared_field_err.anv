// @mode: check
// @expect: error
// @contains: var pattern requires a mutable place
// @frontend: new

extern type Point rep shared {
    x: float;
}

fn main(var p: Point) {
    var Point { x } = p;
    x = 1.0;
}
