// @mode: check
// @expect: error
// @frontend: new
// @contains: does not support struct literal construction

extern type Handle rep shared {
    id: int;
}

fn main() {
    let handle = Handle { id: 1 };
}
