// @mode: check
// @expect: error
// @contains: cannot capture borrowed parameter 'x' in an escaping lambda

extern fn later(f: escaping fn()) -> void;

fn bad(ref x: int) {
    later(|| { x = 1; });
}
