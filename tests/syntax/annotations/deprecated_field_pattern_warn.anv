// @mode: check
// @expect: success
// @warn-contains: use label

struct Item {
    name: string,
    @deprecated("use label")
    old_name: string,
}

fn main() {
    let i = Item { name: "n", old_name: "o" };
    let Item { name, old_name } = i;
    let _ = name;
    let _ = old_name;
}
