// @mode: check
// @expect: error
// @contains: variant 'Event.Move' is not a unit variant

enum Event { Move { dx: int } }

fn main() {
    let event = Event.Move { dx: 1 };
    match event {
        Event.Move => {}
    }
}
