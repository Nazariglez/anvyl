// @mode: check
// @expect: error
// @contains: variant 'State.Idle' is not a struct variant

enum State { Idle }

fn main() {
    let state = State.Idle;
    match state {
        State.Idle {} => {}
    }
}
