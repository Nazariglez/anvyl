// @mode: check
// @expect: error
// @contains: variant 'Message.Data' is not a unit variant

enum Message { Data(int) }

fn main() {
    let msg = Message.Data(1);
    match msg {
        Message.Data => {}
    }
}
