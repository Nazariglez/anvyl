// @mode: check
// @expect: error
// @contains: ref pattern requires a mutable place
// @frontend: new

fn next() -> int? { return 1; }

fn main() {
    match ref next() {
        Option.Some(value) => { value = 1; },
        Option.None => {},
    }
}
