// @mode: check
// @expect: error
// @contains: mutable alias binding

fn main() {
    var opt: int? = 1;
    match ref opt {
        value? => {
            println(opt ?? 0);
            value = 2;
        },
        nil => {},
    };
}
