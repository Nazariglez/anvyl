// @mode: check
// @expect: error
// @contains: cannot assign to immutable value

fn main() {
    var opt: int? = 42;
    match opt {
        Option.Some(value) => { value = 1; },
        Option.None => {},
    }
}
