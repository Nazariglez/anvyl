// @mode: check
// @expect: error
// @contains: mutating method

struct Counter {
    n: int,

    fn inc(ref self) { self.n = self.n + 1; }
}

fn main() {
    let c = Counter { n: 0 };
    c.inc();
}
