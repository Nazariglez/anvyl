// @mode: check
// @expect: error
// @contains: duplicate static method 'make'

struct Counter {
    amount: int,

    fn make(start: int) -> Counter { Counter { amount: start } }
    fn make() -> Counter { Counter { amount: 0 } }
}

fn main() {}
