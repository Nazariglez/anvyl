// @mode: check
// @expect: success

struct Counter {
    n: int,

    fn inc(ref self) { self.n = self.n + 1; }
}

struct Holder { c: Counter }

fn main() {
    var h = Holder { c: Counter { n: 0 } };
    h.c.inc();

    var xs = [Counter { n: 0 }];
    xs[0].inc();
}
