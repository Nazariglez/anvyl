// @mode: check
// @expect: success

struct FixedBuf<T, N: int> {
    data: [T; N],
}

fn main() {
    let a = FixedBuf { data: [1, 2, 3] };
    let b = FixedBuf { data: ["a", "b"] };
    let _: FixedBuf<int, 3> = a;
    let _: FixedBuf<string, 2> = b;
}
