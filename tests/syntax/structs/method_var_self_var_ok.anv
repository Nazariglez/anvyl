// @mode: check
// @expect: success
// @frontend: new

struct Counter { n: int }

extend Counter {
    fn inc(var self) { self.n = self.n + 1; }
}

fn main() {
    var c = Counter { n: 0 };
    c.inc();
}
