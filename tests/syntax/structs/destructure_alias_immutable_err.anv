// @mode: check
// @frontend: new
// @expect: error
// @contains: var pattern requires a mutable place

struct Point { x: int, y: int }

fn main() {
    let p = Point { x: 1, y: 2 };
    var Point { x } = p;
    x = 3;
}
