// @mode: check
// @expect: success
// @frontend: new

struct Counter {
    amount: int,

    fn value(self) -> int { self.amount }
    fn value(start: int) -> Counter { Counter { amount: start } }
}

fn main() {
    let c = Counter.value(3);
    let x: int = c.value();
}
