// @mode: check
// @frontend: new
// @expect: error
// @contains: wrong number of type parameters

struct FixedBuf<T, N: int> {
    data: [T; N],
}

fn main() {
    let buf = FixedBuf { data: [1, 2, 3] };
    let _: FixedBuf<int> = buf;
}
