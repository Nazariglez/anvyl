// @mode: check
// @expect: error
// @frontend: new
// @contains: wrong number of type parameters

struct Box<T> { value: T }
type BoxAlias<T> = Box<T>;

fn f(b: Box<int>) {
    let BoxAlias { value } = b;
}
