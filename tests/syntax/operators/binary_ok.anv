// @mode: check
// @expect: success

fn main() {
    let a: int = 10 + 10;
    let b: int = 10 - 10;
    let c: int = 10 / 10;
    let d: int = 10 * 10;
    let e: int = 10 % 10;
    let f: int = a + b - c;
    let g: int = f * d / e;
    let h: int = f + (a + -20) * (10 - 5) / c;
}