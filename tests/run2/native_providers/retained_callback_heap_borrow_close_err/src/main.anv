// @mode: run
// @frontend: new
// @expect: error
// @stderr-contains: cannot reenter runtime while heap borrow guard is active

import pkg:host.host { retain, close_with_heap_borrow };

fn main() {
    retain(|| println("should not run"));
    close_with_heap_borrow();
}
