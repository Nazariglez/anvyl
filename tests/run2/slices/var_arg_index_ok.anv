// @mode: run
// @frontend: new
// @expect: success
// @match: 2

fn bump(var x: int) {
    x += 1;
}

fn bump_slice(var xs: slice[int], i: int) {
    bump(xs[i]);
}

fn main() {
    var xs: [int] = [0, 1, 2];
    bump_slice(xs, 1);
    println(xs[1]);
}
