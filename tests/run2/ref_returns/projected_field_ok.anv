// @mode: run
// @frontend: new
// @expect: success
// @skip: place returns are not executable yet
// @match: 2

struct Box { value: int }

fn value(ref box: Box) -> ref int {
    box.value
}

fn main() {
    var box = Box { value: 1 };
    value(box) = 2;
    println(box.value);
}
