// @mode: run
// @frontend: new
// @expect: error
// @skip: blocked by promoted collection receiver owner/RIR descriptor before runtime guard
// @contains: cannot structurally mutate collection during active iteration or slice view

fn main() {
    var m: [string: int] = ["a": 1, "b": 2];
    m.retain(|key, value| {
        m.insert("c", 3);
        true
    });
}
