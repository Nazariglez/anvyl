// @mode: run
// @frontend: new
// @expect: success
// @match: 3

fn main() {
    var counts: [string: int] = ["a": 1];
    while ref value? = counts["a"] {
        value += 1;
        if value == 3 {
            break;
        }
    }
    println(counts["a"] ?? 0);
}
