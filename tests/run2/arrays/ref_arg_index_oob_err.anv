// @mode: run
// @frontend: new
// @expect: error
// @stderr-contains: array index 2 out of bounds for len 2

fn bump(ref x: int) {
    x += 1;
}

fn main() {
    var xs: [int; 2] = [0, 1];
    let i = 2;
    bump(xs[i]);
}
