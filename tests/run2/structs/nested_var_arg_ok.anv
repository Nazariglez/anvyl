// @mode: run
// @frontend: new
// @expect: success
// @match: 2

struct Inner {
    x: int,
}

struct Outer {
    pair: (Inner, int),
}

fn bump(var x: int) {
    x += 1;
}

fn main() {
    var outer = Outer { pair: (Inner { x: 1 }, 10) };
    bump(outer.pair.0.x);
    println(outer.pair.0.x);
}
