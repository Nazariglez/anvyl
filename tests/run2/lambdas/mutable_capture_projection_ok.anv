// @mode: run
// @frontend: new
// @expect: success
// @skip: projected writes through captured mutable values are not executable yet
// @match: 1

struct Point { x: int }

fn main() {
    var p = Point { x: 0 };
    let f = || {
        p.x = 1;
    };
    f();
    println(p.x);
}
