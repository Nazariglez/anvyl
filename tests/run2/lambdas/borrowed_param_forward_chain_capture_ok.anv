// @mode: run
// @frontend: new
// @expect: success
// @match: 1

fn leaf(ref x: int) {
    let f = || {
        x += 1;
    };
    f();
}

fn mid(ref x: int) {
    leaf(x);
}

fn top(ref x: int) {
    mid(x);
}

fn main() {
    var n = 0;
    top(n);
    println(n);
}
