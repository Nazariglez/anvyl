// @mode: run
// @frontend: new
// @expect: success
// @skip: mutating receiver capture promotion remains a frontend capture-cell gap
// @match: 1

fn main() {
    var xs: [int] = [0];
    let f = || {
        xs.push(1);
    };
    f();
    println(xs[1]);
}
