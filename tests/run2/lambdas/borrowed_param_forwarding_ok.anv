// @mode: run
// @frontend: new
// @expect: success
// @match: 1

fn inc(var x: int) {
    x += 1;
}

fn touch(var count: int) {
    let f = || {
        inc(count);
    };
    f();
}

fn main() {
    var count = 0;
    touch(count);
    println(count);
}
