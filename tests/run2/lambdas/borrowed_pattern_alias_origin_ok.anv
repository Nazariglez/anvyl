// @mode: run
// @frontend: new
// @expect: success
// @skip: ref pattern alias lambda capture is not executable yet
// @match: 1

struct Point {
    x: int,
    y: int,
}

fn inc(ref value: int) {
    value += 1;
}

fn touch(ref point: Point) {
    var Point { x } = point;
    let f = || {
        inc(x);
    };
    f();
}

fn main() {
    var p = Point { x: 0, y: 0 };
    touch(p);
    println(p.x);
}
