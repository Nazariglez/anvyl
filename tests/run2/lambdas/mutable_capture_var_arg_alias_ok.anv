// @mode: run
// @frontend: new
// @expect: success
// @match: 2

fn bump(var x: int) {
    x += 1;
}

fn main() {
    var count = 0;
    let f = || { bump(count); };
    let g = f;
    f();
    g();
    println(count);
}
