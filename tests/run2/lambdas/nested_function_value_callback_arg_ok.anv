// @mode: run
// @frontend: new
// @expect: success
// @match: 1

fn call(f: fn()) {
    f();
}

fn main() {
    var count = 0;
    let inc = || {
        count += 1;
    };
    let outer = || {
        call(inc);
    };
    outer();
    println(count);
}
