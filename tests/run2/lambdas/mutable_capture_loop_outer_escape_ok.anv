// @mode: run
// @frontend: new
// @expect: success
// @skip: mutable capture escaping a loop body is not executable yet
// @match-begin
// 1
// 2
// @match-end

fn main() {
    var i = 0;
    var g: fn() -> int = || 0;
    while i < 1 {
        var count = 0;
        let f = || {
            count += 1;
            count
        };
        g = f;
        i += 1;
    }
    println(g());
    println(g());
}
