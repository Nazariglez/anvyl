// @mode: run
// @frontend: new
// @expect: success
// @match: ok

fn tick() { println("ok"); }
fn keep(f: escaping fn()) { f(); }

fn main() {
    let fs: [string: fn()] = ["tick": tick];
    let f? = fs["tick"] else return;
    keep(f);
}
