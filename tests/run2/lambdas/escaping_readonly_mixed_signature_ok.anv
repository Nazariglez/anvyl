// @mode: run
// @expect: success
// @skip: mixed escaping and non-escaping lambda values are not executable yet
// @frontend: new

fn make(seed: int) -> fn() -> int {
    || seed
}

fn use_now() -> int {
    var count = 0;
    let f = || {
        count += 1;
        count
    };
    f()
}

fn main() {
    let f = make(3);
    println(f());
    println(use_now());
}
