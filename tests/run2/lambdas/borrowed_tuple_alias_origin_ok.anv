// @mode: run
// @frontend: new
// @expect: success
// @match: 1

fn inc(var value: int) {
    value += 1;
}

fn touch(var pair: (int, int)) {
    var (x, _) = pair;
    let f = || {
        inc(x);
    };
    f();
}

fn main() {
    var pair = (0, 0);
    touch(pair);
    println(pair.0);
}
