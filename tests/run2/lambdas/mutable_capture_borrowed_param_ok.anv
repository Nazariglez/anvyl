// @mode: run
// @frontend: new
// @expect: success
// @match: 1

fn touch(ref count: int) {
    let f = || {
        count += 1;
    };
    f();
}

fn main() {
    var count = 0;
    touch(count);
    println(count);
}
