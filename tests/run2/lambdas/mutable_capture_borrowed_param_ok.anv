// @mode: run
// @frontend: new
// @expect: success
// @skip: borrowed var-param captures await lambda_borrowed_mutable_captures
// @match: 1

fn touch(var count: int) {
    let f = || {
        count += 1;
    };
    f();
}

fn main() {
    var count = 0;
    touch(count);
    println(count);
}
