// @mode: run
// @expect: success
// @match: 3
// @frontend: new

fn make(seed: int) -> fn() -> int {
    || seed
}

fn outer(seed: int) -> fn() -> int {
    let f = make(seed);
    || f()
}

fn main() {
    let f = outer(3);
    println(f());
}
