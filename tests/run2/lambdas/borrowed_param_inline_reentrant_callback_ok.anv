// @mode: run
// @frontend: new
// @expect: success
// @match: 12

fn apply(var x: int, cb: fn()) {
    x += 1;
    cb();
    x += 1;
}

fn touch(var x: int) {
    let f = || {
        apply(x, || {
            x += 10;
        });
    };
    f();
}

fn main() {
    var n = 0;
    touch(n);
    println(n);
}
