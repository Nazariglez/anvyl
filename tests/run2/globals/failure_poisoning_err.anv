// @mode: run
// @frontend: new
// @expect: error
// @skip: no source-level scalar initializer currently returns RuntimeError without panicking
// @stderr-contains: poisoned lazy global

fn fail() -> int {
    var xs: [int; 2] = [0, 1];
    xs[2]
}

lazy let score = fail();

fn main() {
    println(score);
}
