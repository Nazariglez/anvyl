// @mode: run
// @frontend: new
// @expect: success
// @match: 1

enum Slot {
    Item(int),
}

fn consume(slot: Slot) -> int {
    1
}

lazy let slot = Slot.Item(8);

fn main() {
    let local = slot;
    println(consume(local));
}
