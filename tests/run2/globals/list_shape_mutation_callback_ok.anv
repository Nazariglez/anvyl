// @mode: run
// @frontend: new
// @expect: success
// @skip: global collection callback filters need collection-effect lowering through global place plans
// @match-begin
// 1
// 3
// @match-end

lazy var values: [int] = [1, 2, 3];
lazy var removed = 2;

fn main() {
    values.retain(|value| value != removed);
    for value in values {
        println(value);
    }
}
