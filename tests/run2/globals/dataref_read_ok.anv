// @mode: run
// @frontend: new
// @expect: success
// @match: 5

dataref Node {
    value: int,
}

fn bump(var node: Node) {
    node.value += 1;
}

lazy let root: Node = Node { value: 4 };

fn main() {
    var first = root;
    let second = root;
    bump(first);
    println(second.value);
}
