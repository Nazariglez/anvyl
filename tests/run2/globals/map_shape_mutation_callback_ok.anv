// @mode: run
// @frontend: new
// @expect: success
// @skip: global collection callback filters need collection-effect lowering through global place plans
// @match-begin
// a
// 1
// @match-end

lazy var values: [string: int] = ["a": 1, "b": 2];
lazy var removed = 2;

fn main() {
    values.remove_where(|key, value| value == removed);
    for key, value in values {
        println(key);
        println(value);
    }
}
