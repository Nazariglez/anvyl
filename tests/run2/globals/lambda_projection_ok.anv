// @mode: run
// @frontend: new
// @expect: success
// @match: 1

struct Holder {
    f: fn() -> int,
}

fn one() -> int { 1 }

lazy let holder: Holder = Holder { f: one };

fn main() {
    let f = holder.f;
    println(f());
}
