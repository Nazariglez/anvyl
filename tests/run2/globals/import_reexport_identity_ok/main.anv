// @mode: run
// @frontend: new
// @expect: success
// @match-begin
// init
// ready
// ready
// ready
// @match-end

import store;
import facade { title };

fn main() {
    let direct = store.title;
    let reexported = title;
    let via_function = store.get_title();
    println(direct);
    println(reexported);
    println(via_function);
}
