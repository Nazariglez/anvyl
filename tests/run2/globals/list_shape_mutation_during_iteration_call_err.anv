// @mode: run
// @frontend: new
// @expect: error
// @contains: cannot structurally mutate collection during active iteration or slice view

lazy var values: [int] = [1, 2];

fn push_global() {
    values.push(3);
}

fn main() {
    for value in values {
        push_global();
        println(value);
    }
}
