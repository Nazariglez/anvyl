// @mode: run
// @frontend: new
// @expect: error
// @contains: cannot structurally mutate collection during active iteration or slice view

struct State {
    values: [int],
}

lazy var state = State { values: [1, 2] };

fn push_global() {
    state.values.push(3);
}

fn main() {
    for value in state.values {
        push_global();
        println(value);
    }
}
