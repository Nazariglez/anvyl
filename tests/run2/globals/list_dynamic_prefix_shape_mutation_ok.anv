// @mode: run
// @frontend: new
// @expect: success
// @skip: dynamic-prefix global collection shape mutation needs projected global collection mutation

struct State {
    rows: [[int]],
}

lazy var state = State { rows: [[1]] };

fn main() {
    let i = 0;
    state.rows[i].push(2);
}
