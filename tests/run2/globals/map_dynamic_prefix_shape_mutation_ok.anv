// @mode: run
// @frontend: new
// @expect: success

struct State {
    rows: [[string: int]],
}

lazy var state = State { rows: [["a": 1]] };

fn main() {
    let i = 0;
    state.rows[i].insert("b", 2);
}
