// @mode: run
// @frontend: new
// @expect: success
// @match: 9

lazy var values: [int; 3] = [1, 2, 3];

fn main() {
    for var value in values {
        value += 1;
    }
    let local = values;
    println(local[0] + local[1] + local[2]);
}
