// @mode: run
// @frontend: new
// @expect: success
// @match: 2

struct Point { x: int }

fn main() {
    var xs: [Point] = [Point { x: 1 }];
    for var p in xs {
        p.x += 1;
    }
    for p in xs {
        println(p.x);
    }
}
