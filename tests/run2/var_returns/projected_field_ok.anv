// @mode: run
// @frontend: new
// @expect: success
// @skip: clean Rust backend place-return ABI is delegated to mutable place returns owner
// @match: 2

struct Box { value: int }

fn value(var box: Box) -> var int {
    box.value
}

fn main() {
    var box = Box { value: 1 };
    value(box) = 2;
    println(box.value);
}
