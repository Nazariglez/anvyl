// @mode: run
// @frontend: new
// @expect: success
// @match: 2

fn main() {
    var xs: [int?] = [1];
    {
        var value? = xs[0] else return;
        value = 2;
    }
    if let value? = xs[0] {
        println(value);
    }
}
