// @mode: run
// @frontend: new
// @expect: success
// @skip: optional payload aliases over projected map slots need projected-place owner
// @match: 2

fn main() {
    var counts: [string: int] = ["a": 1];
    {
        var value? = counts["a"] else return;
        value = 2;
    }
    if let value? = counts["a"] {
        println(value);
    }
}
