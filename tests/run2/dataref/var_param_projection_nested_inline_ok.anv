// @mode: run
// @frontend: new
// @expect: success
// @match: 7

struct Stats {
    hp: int,
}

dataref Node {
    stats: Stats,
}

fn bump(var hp: int) {
    hp += 2;
}

fn main() {
    var node = Node { stats: Stats { hp: 5 } };
    bump(node.stats.hp);
    println(node.stats.hp);
}
