// @mode: run
// @frontend: new
// @skip: clean rust: enum equality binary lowering is not ready
// @expect: success
// @match: true

enum Choice { One, Two }

fn main() {
    println(Choice.One == Choice.One);
}
