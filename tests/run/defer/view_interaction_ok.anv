// @mode: run
// @expect: success
// @skip: todo -> defer_view
// @match-begin
// 20
// 2
// @match-end

fn main() {
    let items = [10, 20, 30, 40];
    let view = items[1..3];
    defer {
        println(view[0]);
        println(view.len());
    }
}
