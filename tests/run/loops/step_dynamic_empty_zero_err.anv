// @mode: run
// @expect: error
// @stderr-contains: step_by requires a positive count

fn stride() -> int { 0 }

fn main() {
    let xs: [int] = [];
    for n in iter(xs).step_by(stride()) {
        return;
    }
}
