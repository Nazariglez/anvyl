// @mode: run
// @expect: success
// @match: 1

fn inc(ref x: int) {
    x += 1;
}

fn touch(ref count: int) {
    let f = || {
        inc(count);
    };
    f();
}

fn main() {
    var count = 0;
    touch(count);
    println(count);
}
