// @mode: run
// @expect: success
// @match: 1

struct Point { x: int }

fn make() -> fn() {
    var p = Point { x: 0 };
    || {
        p.x = 1;
        println(p.x);
    }
}

fn main() {
    let f = make();
    f();
}
