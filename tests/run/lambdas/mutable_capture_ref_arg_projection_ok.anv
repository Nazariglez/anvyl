// @mode: run
// @expect: success
// @match: 1

struct Point { x: int }

fn inc(ref x: int) {
    x += 1;
}

fn print_x(ref p: Point) {
    println(p.x);
}

fn main() {
    var p = Point { x: 0 };
    let f = || {
        inc(p.x);
    };
    f();
    print_x(p);
}
