// @mode: run
// @expect: success
// @match: 8

fn make(seed: int) -> fn() -> int {
    || seed
}

fn call(f: fn() -> int) -> int {
    let alias = f;
    let g = |x: int| alias() + x;
    g(5)
}

fn main() {
    println(call(make(3)));
}
