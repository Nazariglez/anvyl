// @mode: run
// @expect: success
// @contains: 2

fn main() {
    var count = 0;
    let outer = || {
        let inner = || {
            count += 1;
        };
        inner();
    };
    outer();
    outer();
    println(count);
}
