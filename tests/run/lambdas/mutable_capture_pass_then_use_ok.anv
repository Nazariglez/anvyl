// @mode: run
// @expect: success
// @contains: 2

fn call_once(f: fn()) {
    f();
}

fn main() {
    var count = 0;
    let f = || {
        count += 1;
    };
    call_once(f);
    f();
    println(count);
}
