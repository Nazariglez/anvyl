// @mode: run
// @expect: success
// @match: 4

struct Box { item: int? }

fn main() {
    var box = Box { item: 1 };
    if ref value? = box.item {
        value = 4;
    }
    println(box.item ?? 0);
}
