// @mode: run
// @expect: success
// @match: 1

lazy let stored: fn() -> int = || 1;

fn apply(ref x: int, cb: fn() -> int) {
    cb();
}

fn touch(ref x: int) {
    let scoped = || {
        apply(x, || {
            x += 1;
            x
        });
    };
    scoped();
}

fn main() {
    var n = 0;
    touch(n);
    println(stored());
}
