// @mode: run
// @expect: error
// @stderr-contains: poisoned lazy global
// @stderr-contains: array index 2 out of bounds for len 2

fn fail() -> int {
    var xs: [int; 2] = [0, 1];
    let i = 2;
    xs[i]
}

lazy let score = fail();

fn main() {
    println(score);
}
