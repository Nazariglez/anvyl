// @mode: run
// @expect: success
// @skip: todo -> dataref_var_param
// @match: [1, 2, 1]

dataref State {
    items: [int],
}

fn main() {
    var items: [int] = [1, 2];
    var s = State { items: items };
    s.items.push(s.items[0]);
    println(s.items);
}
