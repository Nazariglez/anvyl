// @mode: run
// @expect: success
// @match: 7

struct Stats {
    hp: int,
}

dataref Node {
    stats: Stats,
}

fn bump(ref hp: int) {
    hp += 2;
}

fn main() {
    var node = Node { stats: Stats { hp: 5 } };
    bump(node.stats.hp);
    println(node.stats.hp);
}
