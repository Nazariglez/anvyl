// @mode: run
// @expect: success
// @match: 7

contract Scored {
    fn score(self) -> int;
}

struct Sprite {
    fn score(self) -> int {
        7
    }
}

fn invoke(actor: dyn Scored) -> int {
    actor.score()
}

fn main() {
    println(invoke(Sprite {}));
}
