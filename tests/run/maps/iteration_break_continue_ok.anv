// @mode: run
// @expect: success
// @match-begin
// c
// a
// @match-end

fn main() {
    let map = ["a": 1, "b": 2, "c": 3];
    for k, v in iter(map).rev().step_by(1) {
        if k == "b" { continue; }
        println(k);
        if k == "a" { break; }
    }
}
