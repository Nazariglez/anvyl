// @mode: run
// @expect: error
// @contains: step_by requires a positive count

fn zero() -> int { 0 }

fn main() {
    let map: [string: int] = [:];
    for k, v in iter(map).step_by(zero()) {
        println(k);
    }
}
