// @mode: run
// @expect: success
// @match: 11

fn next(ref i: int) -> int {
    let old = i;
    i += 1;
    old
}

fn bump(ref x: int) {
    x += 1;
}

fn main() {
    var xs: [int; 2] = [0, 10];
    var i = 0;
    bump(xs[next(i)]);
    println(i * 10 + xs[0]);
}
