// @mode: run
// @expect: success
// @match: 1

enum State: int {
    Idle = 0,
    Run = 1,
}

fn main() {
    if let state = 1 as? State {
        println(state as int);
    }
    if let state = 2 as? State {
        println(state as int);
    }
}
