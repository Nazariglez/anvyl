// @mode: run
// @expect: error
// @stderr-contains: missing retained callback

import pkg:host.host { retain, fire, close };

fn main() {
    retain(|| println("closed"));
    close();
    fire();
}
