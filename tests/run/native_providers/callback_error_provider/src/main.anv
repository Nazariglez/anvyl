// @mode: run
// @expect: error
// @stderr-contains: array index 99 out of bounds for len 2

import pkg:host.host { host_apply };

fn main() {
    let xs: [int; 2] = [1, 2];
    host_apply(|i: int| { xs[i] });
}
