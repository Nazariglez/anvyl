// @mode: run
// @expect: error
// @contains: UnsupportedMutablePlaceNativeBoundary

import pkg:host.host { Counter, make_counter };

struct Holder {
    counter: Counter,
}

fn main() {
    var holder = Holder { counter: make_counter(1) };
    holder.counter.bump(1);
}
