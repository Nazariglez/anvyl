[package]
name = "final_abi_provider_import_err_provider"
version = "0.1.0"
edition.workspace = true

[lib]
crate-type = ["rlib"]

[dependencies]
anvyx-runtime = { path = "../../../../../crates/runtime" }
