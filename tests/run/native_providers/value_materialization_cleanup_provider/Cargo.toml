[package]
name = "value-materialization-cleanup-provider"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["rlib", "cdylib"]

[dependencies]
anvyx-runtime = { path = "../../../../crates/runtime" }
