// @mode: run
// @expect: success
// @match: hello

const MESSAGE: string = "hello";

fn take(message: string?) {
    if let text? = message {
        println(text);
    }
}

fn main() { take(MESSAGE); }
