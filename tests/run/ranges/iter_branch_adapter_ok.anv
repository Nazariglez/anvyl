// @mode: run
// @expect: success
// @match-begin
// 3
// 2
// @match-end

fn maybe(reverse: bool) -> iter {
    if reverse { iter(0..4).rev() } else { iter(0..4) }
}

fn main() {
    let flag = true;
    for n in maybe(flag).take(2) {
        println(n);
    }
}
