// @mode: run
// @expect: error
// @contains: skip requires a non-negative count

fn nums<S>(source: S, count: int) -> iter {
    let n = count;
    iter(source).skip(n)
}

fn main() {
    for n in nums(0..3, -1) {
        println(n);
    }
}
