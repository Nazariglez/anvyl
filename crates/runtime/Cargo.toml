[package]
name = "anvyx-runtime"
version.workspace = true
edition.workspace = true

[dependencies]
anvyx-externs = { path = "../externs" }
anvyx-heap = { path = "../heap" }
anvyx-macros = { path = "../macros" }
ecow = "0.2.6"
indexmap = "2"
inventory = "0.3"
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
trybuild = "1"

[lints]
workspace = true
