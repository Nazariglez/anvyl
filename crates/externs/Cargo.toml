[package]
name = "anvyx-externs"
version.workspace = true
edition.workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }
unicode-ident = "1"

[dev-dependencies]
serde_json = "1"

[lints]
workspace = true
